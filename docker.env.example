# Copiar a docker.env (junto a este archivo). docker.env está en .gitignore del repo.
# Sobrescribe DATABASE_URL solo cuando corres docker compose: Postgres en tu PC → host del host.
DATABASE_URL=postgresql://postgres:CAMBIA_PASSWORD@host.docker.internal:5432/Daia

# En Docker Compose, User llama a Academy por la red interna (no uses https://academy.nss.lat aquí).
# ACADEMY_API_URL=http://academy-api:8000
